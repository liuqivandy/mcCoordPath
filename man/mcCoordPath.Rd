% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcCoordPath.R
\name{mcCoordPath}
\alias{mcCoordPath}
\title{Identify multicellular coordinated pathways}
\usage{
mcCoordPath(
  data_list,
  pathMat,
  scale = FALSE,
  allGenes = TRUE,
  k = NULL,
  L1 = NULL,
  L2 = NULL,
  L3 = NULL,
  L4 = NULL,
  L3_method = c("mean", "min", "max", "median"),
  frac = 0.7,
  maxPath = 10,
  minGenes = 10,
  doCrossval = TRUE,
  glm_alpha = 0.9,
  max.iter = 350,
  trace = FALSE,
  tol = 1e-06,
  seed = 123456
)
}
\arguments{
\item{data_list}{A list of gene expression matrices, one per cell type, with genes in rows and samples in columns.}

\item{pathMat}{A binary gene–pathway matrix (genes × pathways) defining gene–pathway membership.}

\item{scale}{Logical; whether to scale gene expression within each cell type. Default = FALSE.}

\item{allGenes}{Logical; whether to use all genes (TRUE) or only those appearing in `pathMat` (FALSE). Default = TRUE.}

\item{k}{Integer or NULL; number of latent factors. If NULL, chosen automatically.}

\item{L1}{Numeric or NULL; penalty controlling the reconstruction of gene loadings from pathway priors (‖Z − C·U‖²). Default = NULL (auto-selected).}

\item{L2}{Numeric or NULL; ridge penalty on the shared latent activity matrix `B` (‖B‖²). Default = NULL (auto-selected).}

\item{L3}{Numeric or NULL; lasso penalty on pathway weights `U` to encourage pathway sparsity. Default = NULL (auto-selected).}

\item{L4}{Numeric or NULL; ridge penalty on cell-type–specific latent factors `V`. Default = NULL (auto-selected).}

\item{L3_method}{Character; method to combine automatically chosen L3 values across cell types (e.g., `"mean"`, `"median"`). Default = "mean".}

\item{frac}{Numeric (0–1); proportion of latent factors required to have pathway support. Default = 0.7.}

\item{maxPath}{Integer; maximum number of pathways per latent factor. Default = 10.}

\item{minGenes}{Integer; minimum number of genes per pathway. Default = 10.}

\item{doCrossval}{Logical; whether to perform cross-validation to tune penalties. Default = TRUE.}

\item{glm_alpha}{Numeric; alpha parameter for elastic-net penalty in glmnet. Default = 0.9.}

\item{max.iter}{Integer; maximum number of iterations for model fitting. Default = 350.}

\item{trace}{Logical; whether to display progress information. Default = FALSE.}

\item{tol}{Numeric; convergence threshold. Default = 1e-6.}

\item{seed}{Integer; random seed for reproducibility. Default = 123456.}
}
\value{
A list with the following components:
\describe{
  \item{B}{Matrix of coordinated (shared) latent factors (LFs × samples).}
  \item{Z_list}{List of gene–latent factor loading matrices (\(Z_v\)) for each cell type.}
  \item{U_list}{List of pathway–latent factor matrices (\(U_v\)) for each cell type.}
  \item{V_list}{List of cell-type–specific latent factor matrices (\(V_v\)).}
  \item{AUC_list}{List of AUC or pathway activity quality metrics per cell type.}
  \item{L1}{The effective L1 penalty value used in the model.}
  \item{L2}{The effective L2 penalty value used in the model.}
  \item{L3}{The effective L3 penalty value used in the model.}
  \item{L4}{The effective L4 penalty value used in the model.}
  \item{Vratio}{Variance ratio of cell-type–specific patterns (`V`) relative to coordinated patterns (`B`).}
  \item{heldOutGenes}{Vector of genes held out during cross-validation (if `doCrossval = TRUE`).}
}
}
\description{
This function identifies coordinated and cell-type–specific pathway activities
across multiple cell types using a multi-view latent factor model constrained
by gene–pathway priors.
}
