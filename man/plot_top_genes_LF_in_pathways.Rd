% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcCoordPath_vis.R
\name{plot_top_genes_LF_in_pathways}
\alias{plot_top_genes_LF_in_pathways}
\title{Plot top genes in enriched pathways for a latent factor}
\usage{
plot_top_genes_LF_in_pathways(
  coPathRes,
  pathMat,
  lf_index = 1,
  topn = 10,
  auc_threshold = 0.7,
  fdr_threshold = 0.05,
  scale_rows = FALSE,
  view_names = NULL
)
}
\arguments{
\item{coPathRes}{A result object from \code{mcCoordPath}}

\item{pathMat}{A binary matrix indicating gene membership in pathways}

\item{lf_index}{Integer specifying which latent factor to examine. Default is 1.}

\item{topn}{Integer. Maximum number of top genes per pathway to include in the heatmap. Default is 10.}

\item{auc_threshold}{Numeric. Minimum AUC threshold to consider a pathway enriched. Default is 0.7.}

\item{fdr_threshold}{Numeric. Maximum FDR threshold to consider a pathway enriched. Default is 0.05.}

\item{scale_rows}{Logical. If TRUE, rows of the heatmap are scaled (z-score). Default is FALSE.}

\item{view_names}{Optional character vector specifying names of the views/cell types.
If NULL, defaults to "View1", "View2", etc.}
}
\value{
The function plots a heatmap using \code{pheatmap} showing the top genes
  in enriched pathways for the selected latent factor. Genes are annotated by
  the pathways they belong to across all cell types.
}
\description{
Generate a heatmap of the top genes from pathways enriched in a specific latent
factor (LF) across multiple cell types or views. Only pathways passing AUC and
FDR thresholds are considered, and the top genes per pathway are selected
based on their latent factor loadings.
}
