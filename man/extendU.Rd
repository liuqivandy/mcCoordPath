% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcCoordPath.R
\name{extendU}
\alias{extendU}
\title{Extend pathway–latent factor associations to new pathways}
\usage{
extendU(
  data_list,
  coPathRes,
  newpathMat,
  target.frac = 0.7,
  maxPath = 10,
  minGenes = 10,
  useSparse = TRUE
)
}
\arguments{
\item{data_list}{A list of gene expression matrices, one per cell type, with
genes in rows and samples in columns.}

\item{coPathRes}{The result object from \code{mcCoordPath}}

\item{newpathMat}{A binary gene–pathway matrix (genes in rows, pathways in columns)
representing the new pathways to be evaluated.}

\item{target.frac}{Numeric value between 0 and 1; the fraction of latent factors
expected to have pathway associations. Default = \code{0.7}.}

\item{maxPath}{Integer; maximum number of pathways to retain for each latent
factor after extension. Default = \code{10}.}

\item{minGenes}{Integer; minimum number of genes required in each pathway
to be considered. Default = \code{10}.}

\item{useSparse}{Logical; whether to use sparse matrix operations for efficiency.
Default = \code{TRUE}.}
}
\value{
A list with the same structure as the original \code{mcCoordPath} result,
  but with updated \code{U_list} matrices reflecting the extended pathway annotations.
}
\description{
This function updates or extends the pathway–latent factor mappings (\code{U_list})
from a previous \code{mcCoordPath} result by incorporating a new pathway–gene
annotation matrix. It can be used to test whether additional pathways are associated
with existing coordinated latent factors.
}
